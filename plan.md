# Classic Bubble Shooter 웹게임 제작 기획서

## 1. 프로젝트 목표

HTML5, CSS3, Vanilla JavaScript만 사용해 별도 설치나 빌드 과정 없이 브라우저에서 바로 실행할 수 있는 Classic Bubble Shooter 게임을 제작한다. 플레이어는 하단 발사대에서 버블을 조준해 발사하고, 같은 색 버블을 3개 이상 연결해 제거한다. 연결이 끊긴 공중 버블도 함께 떨어뜨려 높은 점수를 얻는다.

## 2. 기술 및 실행 환경

- HTML5, CSS3, JavaScript ES6+만 사용한다.
- React, Vue, Canvas 게임 라이브러리, 물리 엔진, Bootstrap, Tailwind 등 외부 라이브러리는 사용하지 않는다.
- `index.html`을 브라우저에서 직접 열어 실행할 수 있어야 한다.
- 다음 파일로 분리한다.
	- `index.html`: 게임 화면과 접근성 구조
	- `style.css`: 게임 UI, 반응형 레이아웃, 효과와 애니메이션
	- `script.js`: 게임 상태, 조준, 발사, 충돌, 매칭, 점수, 라운드 로직
- 메인 게임 보드는 Canvas 2D API 또는 DOM 방식으로 구현할 수 있다. 발사체의 부드러운 이동과 벽 반사를 고려해 Canvas 2D 구현을 권장한다.
- 외부 이미지와 폰트 없이도 실행되어야 하며, 버블은 Canvas의 원형 도형과 색상으로 그린다.

## 3. 화면 구성

### 상단 HUD

- 게임 제목: `Classic Bubble Shooter`
- 현재 점수: `Score: 0000`
- 남은 패널티 또는 기회 표시: `Shots Left` 혹은 버블 아이콘 여러 개
- 다음 천장 행 추가까지 남은 실패 횟수 표시
- 최상단 버블 행이 내려오는 타이머 또는 진행 표시 영역을 한 줄로 배치한다.
- HUD는 데스크톱에서 보드 위에, 모바일에서는 보드 폭에 맞춰 줄바꿈 없이 읽기 쉽게 배치한다.

### 중앙 게임 보드

- 발사체와 버블이 표시되는 메인 보드를 구성한다.
- 보드는 화면 중앙에 배치하고, 버블이 하단 발사대 안전선 아래로 내려오지 않도록 하단 위험선을 표시한다.
- 버블은 반지름을 기준으로 일정한 크기를 유지하고 격자 형태로 배치한다.
- 홀수 행과 짝수 행을 반 버블 지름만큼 오프셋한 육각 인접 구조를 사용한다.
- 보드 양쪽 벽에는 발사 버블이 반사되는 경계를 둔다.

### 하단 발사 영역

- 보드 하단 중앙에 발사대와 현재 발사 버블을 표시한다.
- 발사대 위 또는 옆에 `Next Bubble` 영역을 두고 다음 발사될 버블을 미리 보여준다.
- 조준 중에는 발사대에서 마우스 또는 터치 위치로 향하는 점선 또는 반투명 가이드라인을 보여준다.
- 발사대 안전선 아래에 컨트롤 영역이 겹치지 않도록 한다.

### 종료 및 라운드 오버레이

- Game Over: `Game Over`, 최종 점수, `[다시 시작]` 버튼을 표시한다.
- Clear: `Round Clear!`, 현재 점수, `[다음 라운드]`, `[점수 저장]` 버튼을 표시한다.
- 오버레이가 표시된 동안 발사와 조준 입력을 막는다.

## 4. 버블 규칙

- 총 5가지 색상을 사용한다.
	- 빨강: `#F25555`
	- 파랑: `#4D8DFF`
	- 초록: `#58B878`
	- 노랑: `#F5C84B`
	- 보라: `#A77BDF`
- 각 버블은 색상 외에도 은은한 하이라이트, 테두리, 그림자를 사용해 입체감을 표현한다.
- 게임 시작 시 보드의 상단 일부 행에만 무작위 색상 버블을 배치한다.
- 시작 행 수는 기본 5행으로 설정하되 라운드가 올라가면 1행씩 늘릴 수 있다.
- 보드에 고정된 버블과 발사 중인 버블은 별도의 상태로 관리한다.
- 발사 버블과 Next Bubble 색상은 5가지 색상 중 무작위로 생성한다.

## 5. 조작 방식

- 마우스 이동 또는 터치 이동으로 조준 각도를 변경한다.
- 발사대보다 위쪽을 향하는 방향만 조준할 수 있도록 각도를 제한한다.
- 조준선은 최소 각도와 최대 각도 사이에서만 표시한다.
- 마우스 클릭 또는 터치 떼기(`pointerup`)로 버블을 발사한다.
- `pointermove`, `pointerdown`, `pointerup`, `pointercancel`을 사용해 마우스와 터치 입력을 통합한다.
- 발사 중에는 중복 발사를 막고, 버블이 고정되거나 애니메이션이 끝난 뒤 다음 발사를 허용한다.
- 입력 영역에는 `touch-action: none`을 적용해 화면 스크롤과 게임 조작이 충돌하지 않게 한다.
- 키보드 접근성을 위해 좌우 방향키로 조준 각도를 조절하고, Enter 또는 Space로 발사할 수 있게 한다.

## 6. 발사와 물리 동작

1. 발사 시 현재 버블의 색상을 발사체에 복사하고 Next Bubble을 현재 버블로 승격한다.
2. 발사체는 조준 방향의 단위 벡터를 사용해 일정한 속도로 이동한다.
3. 발사체가 좌우 벽에 닿으면 x 방향 속도의 부호를 반전해 입사각과 같은 반사각으로 튕겨 나간다.
4. 발사체가 보드 상단에 닿거나 기존 버블과 충돌하면 가장 가까운 유효 격자 위치에 고정한다.
5. 발사체가 버블 사이를 통과하거나 보드 밖으로 사라지는 상태가 발생하지 않도록 프레임마다 위치를 검사한다.
6. `requestAnimationFrame`을 사용해 이동과 충돌을 갱신한다.
7. 충돌 시 발사체를 고정 버블로 변환하고 매칭 판정을 시작한다.

충돌 판정은 발사체와 고정 버블 중심 간 거리가 버블 지름 이하인지 검사한다. 고정 위치는 육각 격자의 인접 후보 중 보드 안에 있고 이미 점유되지 않은 위치를 선택한다.

## 7. 매칭 및 버블 제거

- 버블이 보드에 고정되면 고정된 버블과 같은 색이며 상하좌우 및 대각선의 육각 인접 관계로 연결된 버블을 탐색한다.
- 같은 색 연결 그룹이 3개 이상이면 해당 그룹을 제거한다.
- 제거되는 버블 1개마다 10점을 추가한다.
	- 매칭 점수: `제거된 연결 버블 수 × 10`
- 연결 그룹이 3개 미만이면 매칭 실패로 처리한다.
- 매칭 실패 시 패널티 카운트를 1회 차감한다.
- 매칭 성공 시 패널티 카운트를 초기화하거나 라운드별 설정에 따라 일부 회복할 수 있다.
- 제거 애니메이션 중에는 해당 버블의 입력과 충돌 판정을 막는다.

### 공중에 뜬 버블 처리

1. 매칭 그룹을 제거한다.
2. 제거 후 보드 최상단에 연결된 버블을 시작점으로 삼아 모든 인접 버블을 탐색한다.
3. 최상단과 연결되지 않은 버블은 공중에 뜬 버블로 판단한다.
4. 공중에 뜬 버블을 모두 제거한다.
5. 공중에 떠서 제거된 버블 1개마다 20점의 보너스를 추가한다.
	 - 낙하 보너스: `공중 제거 버블 수 × 20`
6. 제거된 버블이 모두 사라진 뒤 다음 발사를 준비한다.

공중 버블은 아래로 떨어지는 애니메이션을 보여준 뒤 보드에서 제거한다. 매칭 버블과 공중 버블을 사용자에게 시각적으로 구분할 수 있으면 좋다.

## 8. 패널티와 천장 행 추가

- 발사 후 매칭에 실패할 때마다 패널티 카운트를 1 차감한다.
- 기본 패널티 최대치는 5회로 설정한다.
- HUD에 현재 남은 패널티와 다음 천장 행까지 남은 실패 횟수를 표시한다.
- 패널티가 0이 되면 다음 발사 전에 천장에 새로운 버블 행을 추가한다.
- 새 버블 행은 보드 최상단에 생성하고, 기존 버블은 한 행 아래로 이동한다.
- 새 행의 색상은 무작위로 생성하며, 현재 라운드에 설정된 색상 수를 따른다.
- 새로운 행을 추가한 뒤 패널티를 기본 최대치로 회복한다.
- 새 행을 추가할 때 짧은 화면 흔들림 또는 경고 애니메이션을 표시한다.
- 버블이 하단 발사대 라인 또는 위험선에 닿으면 즉시 Game Over 처리한다.

## 9. 게임 종료와 라운드

### Game Over

- 버블의 중심 또는 하단 영역이 발사대 위험선까지 내려오면 Game Over로 전환한다.
- Game Over 오버레이에는 최종 점수와 다시 시작 버튼을 표시한다.
- 다시 시작하면 보드, 점수, 패널티, 현재 버블, Next Bubble, 라운드가 초기 상태로 돌아간다.

### Round Clear

- 보드에 고정된 버블이 하나도 남지 않으면 라운드 클리어로 처리한다.
- Clear 메시지와 현재 점수를 표시한다.
- `[다음 라운드]`를 누르면 다음 라운드로 이동한다.
- 다음 라운드는 시작 버블 행 수를 늘리거나 패널티 최대치를 줄여 난이도를 높일 수 있다.
- `[점수 저장]`을 누르면 `localStorage`에 최고 점수를 저장한다.
- 저장된 최고 점수는 게임 시작 화면 또는 HUD에 표시한다.
- `localStorage`를 사용할 수 없는 환경에서도 게임 핵심 기능은 정상 동작해야 한다.

## 10. 화면 상태와 주요 데이터

다음과 같은 상태 구조를 권장한다.

- `gameState`: `ready`, `aiming`, `firing`, `resolving`, `gameover`, `clear`
- `board`: 행과 열 좌표를 가진 고정 버블 배열
- `currentBubble`: 현재 발사할 버블 색상
- `nextBubble`: 다음 발사 버블 색상
- `projectile`: 위치, 속도, 반지름, 색상을 가진 발사체
- `aimAngle`: 현재 조준 각도
- `score`: 현재 점수
- `highScore`: 저장된 최고 점수
- `penaltiesLeft`: 남은 실패 허용 횟수
- `maxPenalties`: 패널티 최대치
- `round`: 현재 라운드
- `isAnimating`: 제거·낙하·천장 추가 애니메이션 여부

## 11. 권장 함수 구조

- `initializeGame()`: 전체 게임 상태와 첫 보드 생성
- `createInitialBoard()`: 상단 일부 행에 버블 배치
- `createBubble(color)`: 버블 객체 생성
- `drawBoard()`: Canvas에 고정 버블, 조준선, 발사대 렌더링
- `drawHud()`: 점수, 패널티, 라운드, Next Bubble 갱신
- `updateAim(pointerPosition)`: 포인터 위치에 따른 조준 각도 계산
- `fireBubble()`: 현재 버블을 발사체로 전환
- `updateProjectile(deltaTime)`: 발사체 이동과 벽 반사 처리
- `detectCollision()`: 상단 또는 기존 버블과 충돌 검사
- `snapProjectileToGrid()`: 발사체를 빈 육각 격자 위치에 고정
- `findConnectedGroup(startCell)`: 같은 색 인접 버블 그룹 탐색
- `removeMatchedBubbles(group)`: 3개 이상 그룹 제거 및 10점 계산
- `findFloatingBubbles()`: 최상단 연결 여부를 기준으로 공중 버블 탐색
- `removeFloatingBubbles(group)`: 공중 버블 제거 및 20점 보너스 계산
- `handleMiss()`: 매칭 실패, 패널티 차감 및 천장 행 추가
- `addCeilingRow()`: 최상단 버블 행 추가
- `checkGameOver()`: 위험선 도달 여부 확인
- `checkRoundClear()`: 보드 잔여 버블 여부 확인
- `showGameOver()`: Game Over 오버레이 표시
- `showRoundClear()`: Clear 오버레이 표시
- `saveHighScore()`: 최고 점수 저장
- `resetGame()`: 초기 상태로 재시작

## 12. 디자인 및 UX 방향

- 클래식 아케이드 느낌의 밝고 선명한 색상과 어두운 보드 대비를 사용한다.
- 버블은 충분한 크기와 간격을 갖고 숫자나 색상 구분이 명확해야 한다.
- 조준선은 버블 색상과 겹치지 않도록 점선과 반투명 흰색 또는 노란색으로 표시한다.
- 발사, 반사, 매칭 제거, 공중 낙하, 천장 행 추가에 짧고 명확한 효과를 적용한다.
- 점수 획득 시 `+10`, 공중 버블 보너스 시 `+20` 텍스트를 표시한다.
- Game Over와 Clear 상태는 색상과 문구가 명확히 달라야 한다.
- 모바일에서 Canvas가 화면 너비를 넘지 않아야 하며 터치 조작이 편해야 한다.
- `prefers-reduced-motion: reduce` 설정에서는 장식 애니메이션을 최소화한다.

## 13. 접근성 및 예외 처리

- Canvas만 사용하는 경우에도 게임 상태, 점수, 패널티, 현재 버블을 별도 HTML 요소로 제공한다.
- 상태 변경은 `aria-live="polite"` 영역으로 알린다.
- 다시 시작, 다음 라운드, 점수 저장 버튼은 키보드로 접근할 수 있어야 한다.
- 키보드 포커스 스타일을 명확하게 표시한다.
- 터치가 보드 밖에서 끝나도 `pointercancel`과 전역 `pointerup`으로 입력 상태가 초기화되어야 한다.
- 발사 중 여러 번 클릭해도 중복 발사되지 않아야 한다.
- 천장 행 추가와 제거 애니메이션 중에는 보드 입력을 잠근다.
- 화면 크기 변경 시 Canvas 해상도와 버블 좌표를 다시 계산하되 게임 상태는 유지한다.
- 콘솔 오류 없이 최신 Chrome, Edge, Firefox에서 실행되어야 한다.

## 14. 완료 기준

- [ ] `index.html`을 직접 열었을 때 게임이 실행된다.
- [ ] 상단 HUD에 Score와 패널티/타이머 영역이 표시된다.
- [ ] 중앙 게임 보드와 하단 Shooter, Next Bubble이 표시된다.
- [ ] 5가지 색상의 버블이 상단 일부 행에 무작위로 생성된다.
- [ ] 마우스와 터치로 조준 각도를 조절할 수 있다.
- [ ] 클릭 또는 터치 떼기로 버블을 발사할 수 있다.
- [ ] 발사 버블이 좌우 벽에서 자연스럽게 반사된다.
- [ ] 기존 버블 또는 최상단에 충돌하면 격자에 고정된다.
- [ ] 같은 색 버블 3개 이상 연결 시 제거된다.
- [ ] 제거 버블 1개당 10점이 추가된다.
- [ ] 최상단과 연결이 끊긴 공중 버블이 제거되고 1개당 20점이 추가된다.
- [ ] 매칭 실패 시 패널티가 차감된다.
- [ ] 패널티 소진 시 새 버블 행이 추가되고 패널티가 회복된다.
- [ ] 버블이 발사대 위험선에 닿으면 Game Over가 표시된다.
- [ ] 모든 버블을 제거하면 Clear 메시지와 다음 라운드/점수 저장 기능이 표시된다.
- [ ] 다시 시작, 다음 라운드, 최고 점수 저장이 정상 동작한다.
- [ ] 모바일과 데스크톱에서 보드와 조작 UI가 깨지지 않는다.
